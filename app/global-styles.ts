import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  titleContainerRow: {
    flexDirection: 'row',
    gap: 8,
  },
  contentBlock: {
    gap: 8,
    marginBottom: 8,
  },
});